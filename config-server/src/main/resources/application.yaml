spring:
  application:
    name: config-server

cloud:
  config:
    server:
      git:
        uri: https://github.com/FacuOlivieri/RepoConfigServer.git
        searchPaths: config-data
        username: ${GIT_USER}
        password: ${GIT_PASSWORD}